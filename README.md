# Student-Utility-Program
Create a Java program that models a Student and performs simple operations using methods.

Student Utility Program

📌 Description
This Java program demonstrates basic Object-Oriented Programming (OOP) concepts by modeling a Student class.  
It stores student details, checks whether a student has passed based on their grade, and displays complete information.

🧩 Program Features
- Uses private attributes for encapsulation
- Includes methods to:
  - Set student details
  - Check pass/fail status
  - Display student information
- Handles multiple student objects

🛠️ How to Run the Program
1. Make sure Java JDK is installed.
2. Save the program as `Student.java`.
3. Open a terminal in the file directory.
4. Compile the program:
   ```bash
   javac Student.java

